# Changelog

## [0.1.0] - Initial scaffold
- MMM-oriented project structure (library + pipelines + artifacts).
- pyproject.toml with optional dependency groups (dev/api/app).
- Optional API and Streamlit placeholders.
